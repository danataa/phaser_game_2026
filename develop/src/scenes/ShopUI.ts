export default class ShopUI extends Phaser.Scene {

  constructor() {
    super({ key: "ShopUI" });
  }

  create() {
    console.log("ShopUI");
  }

  update(time: number, delta: number): void {

  }
}