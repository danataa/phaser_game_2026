import Phaser from "phaser";
import Actor from "./Actor";

export default class Merchant extends Actor {

    private _ragioIterazione: number;
    private _prompt: Phaser.GameObjects.Text;
    private _shopAperto: boolean;

    
}
