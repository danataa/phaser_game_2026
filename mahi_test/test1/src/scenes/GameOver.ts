import { GameData } from "../GameData";

export default class GameOver extends Phaser.Scene {

  /** Scene constructor (key: GameOver). */
  constructor() {
    super({
      key: "GameOver",
    });
  }

  /** Game-over scene entry point (placeholder). */
  create() {
    console.log("GameOver");
  }




}
