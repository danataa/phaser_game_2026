import GamePlay from "./GamePlay";

export default class Hud extends Phaser.Scene {

  /** Scene constructor (key: Hud). */
  constructor() {
    super({
      key: "Hud",
    });
  }

  /** HUD scene entry point (currently unused). */
  create() {
    console.log("HUD");

  }



}
